name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python: ["3.10", "3.11", "3.12", "3.13", "3.14"]
    defaults:
      run:
        shell: bash -el {0}
    steps:
      - uses: actions/checkout@v4

      - uses: conda-incubator/setup-miniconda@v3
        with:
          python-version: ${{ matrix.python }}
          activate-environment: test
          channels: conda-forge

      - name: Install conda-pypi
        run: conda install -n base -y -c conda-forge conda-pypi

      - name: Install conda-tasks with test dependencies
        run: conda pypi install -e ".[test]"

      - name: Run tests
        run: conda task run test
